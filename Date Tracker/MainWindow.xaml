<Window x:Class="Date_Tracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Date_Tracker"
        mc:Ignorable="d"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}"
        Title="Date Tracker" Height="700" Width="500"
        MouseDown="Window_MouseDown"
        Icon="/Assets/logo.ico"
        >
    <Window.Resources>
        <DataTemplate x:Key="timeLeftTemplate">
            <Grid Margin="-6, 0, -6, 0">
                <TextBlock FontSize="11" FontWeight="Bold" FontFamily="Arial" Foreground="{Binding CellForegroundBrush}">
                    <TextBlock.Text>
                        <Binding Path="TimeLeft"/>
                    </TextBlock.Text>
                    <TextBlock.Background>
                        <SolidColorBrush Color="{Binding CellBackgroundBrush}"/>
                    </TextBlock.Background>
                </TextBlock>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="nameTemplate">
            <Grid Margin="-6, 0, -6, 0">
                <TextBlock FontSize="11" FontWeight="Bold" FontFamily="Arial" Foreground="{Binding CellForegroundBrush}">
                    <TextBlock.Text>
                        <Binding Path="Name"/>
                    </TextBlock.Text>
                    <TextBlock.Background>
                        <SolidColorBrush Color="{Binding CellBackgroundBrush}"/>
                    </TextBlock.Background>
                </TextBlock>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="dateTemplate">
            <Grid Margin="-6, 0, -6, 0">
                <TextBlock FontSize="11" FontWeight="Bold" FontFamily="Arial" Foreground="{Binding CellForegroundBrush}">
                    <TextBlock.Text>
                        <Binding Path="DateDisplay"/>
                    </TextBlock.Text>
                    <TextBlock.Background>
                        <SolidColorBrush Color="{Binding CellBackgroundBrush}"/>
                    </TextBlock.Background>
                </TextBlock>
            </Grid>
        </DataTemplate>
    </Window.Resources>
    <Viewbox>
        <Grid Height="700" Width="500">
            <Grid x:Name="calendar_grid" Margin="10,10,0,524">
                <Calendar x:Name="calendar" SelectedDatesChanged="calendar_SelectedDatesChanged" HorizontalAlignment="Left"/>
                <TextBlock FontFamily="Arial" FontSize="15.5" Foreground="{DynamicResource ForegroundBrush}" Margin="184,4,40,142">Select a date to track</TextBlock>
                <StackPanel x:Name="count_panel" Orientation="Horizontal" Margin="184,24,0,120">
                    <TextBlock FontFamily="Arial" FontSize="13.5" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}">Tracking mode:</TextBlock>
                    <RadioButton x:Name="count_up_radio_btn" IsEnabled="False" Margin="5, 0" FontFamily="Arial" FontSize="12.5" Foreground="{DynamicResource ForegroundBrush}" FontWeight="Bold" Content="Time since" IsChecked="True" Height="16"/>
                    <RadioButton x:Name="count_down_radio_btn" FontFamily="Arial" FontSize="12.5" Margin="25, 0" Foreground="{DynamicResource ForegroundBrush}" FontWeight="Bold" Content="Time until" IsChecked="True" Height="16"/>
                </StackPanel>
                <StackPanel x:Name="label_panel" Orientation="Horizontal" Margin="184,46,0,97">
                    <TextBlock FontFamily="Arial" FontSize="13.5" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}" Height="18">Date label:</TextBlock>
                    <TextBox x:Name="label_box" FontFamily="Arial" TextChanged="label_box_TextChanged" Background="{DynamicResource BackgroundBrush}" FontSize="12" FontWeight="Normal" Foreground="{DynamicResource ForegroundBrush}" BorderBrush="{DynamicResource ForegroundBrush}" TextWrapping="WrapWithOverflow" Width="237" Height="18"/>
                </StackPanel>
                <StackPanel x:Name="timezone_panel" Visibility="Hidden" Orientation="Horizontal" Margin="184,69,0,78">
                    <TextBlock FontFamily="Arial" FontSize="13.5" FontWeight="Bold">Timezone: </TextBlock>
                    <RadioButton x:Name="use_crnt_timezone_btn" FontFamily="Arial" Margin="5, 0" Content="Use current (GMT+2)" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}" IsChecked="True" Click="use_crnt_timezone_btn_Click"/>
                    <RadioButton x:Name="use_diff_timezone_btn" FontFamily="Arial" Margin="5, 0" Content="Other" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}" IsChecked="False" Click="use_diff_timezone_btn_Click"/>
                </StackPanel>
                <StackPanel x:Name="diff_timezone_panel" Orientation="Horizontal" Margin="184,83,0,64" Visibility="Hidden">
                    <TextBlock FontFamily="Arial" FontSize="13.5" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}">Select a new timezone: </TextBlock>
                    <ComboBox x:Name="new_timezone_combo" BorderBrush="{DynamicResource ForegroundBrush}" SelectedIndex="0" ItemsSource="{Binding Timezones}" Width="158"/>
                </StackPanel>
                <StackPanel x:Name="add_to_panel" Orientation="Horizontal" Margin="184,102,0,47">
                    <TextBlock FontFamily="Arial" FontSize="12.5" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}">Add to:</TextBlock>
                    <RadioButton x:Name="favourite_radio_btn" FontSize="12" FontWeight="Bold" Margin="10, 0" FontFamily="Arial" Foreground="{DynamicResource ForegroundBrush}" Content="Favourites" Click="favourite_radio_btn_Click"/>
                    <RadioButton x:Name="pinned_radio_btn" FontSize="12" FontWeight="Bold" Margin="15, 0" FontFamily="Arial" Foreground="{DynamicResource ForegroundBrush}" Content="Pinned" Click="pinned_radio_btn_Click"/>
                    <RadioButton x:Name="none_radio_btn" FontSize="12" FontWeight="Bold" Margin="15, 0" FontFamily="Arial" IsChecked="True" Foreground="{DynamicResource ForegroundBrush}" Content="None" Click="none_radio_btn_Click"/>
                </StackPanel>
                <CheckBox x:Name="delete_when_reached_checked_box_item" FontSize="12.5" FontFamily="Arial" FontWeight="Bold" Click="delete_when_reached_check" Foreground="{DynamicResource ForegroundBrush}" Content="Delete when date is reached" Margin="184,119,0,30"/>
                <Button x:Name="add_btn" IsEnabled="False" FontSize="12.5" FontWeight="Bold" FontFamily="Arial" Click="add_btn_Click" Foreground="White" Background="#052099" BorderThickness="0" Content="Track date" Margin="184,136,0,6"/>
            </Grid>
            <StackPanel x:Name="filter_panel" Orientation="Horizontal" Margin="10,181,10,489">
                <TextBox x:Name="search_box" FontFamily="Arial" FontSize="12" FontWeight="Bold" TextWrapping="WrapWithOverflow" Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource ForegroundBrush}" Foreground="{DynamicResource ForegroundBrush}" Width="250" Height="21"/>
                <Button x:Name="search_btn" FontFamily="Arial" Margin="5, 0, 0, 0" FontSize="12" FontWeight="Bold" Background="#052099" Foreground="White" BorderThickness="0" Click="search_btn_Click" Content="Search" Height="18" Width="61"/>
                <CheckBox x:Name="favourite_only_check" FontFamily="Arial" FontSize="12" FontWeight="Bold" Margin="15, 0" Foreground="{DynamicResource ForegroundBrush}" Click="favourite_only_check_Click" Content="Favourites only" Height="16"/>
            </StackPanel>
            <ListView x:Name="date_list" SelectionChanged="date_list_SelectionChanged" ItemsSource="{Binding DateList}" BorderBrush="{DynamicResource ForegroundBrush}" Background="{DynamicResource BackgroundBrush}" Foreground="{DynamicResource ForegroundBrush}" Margin="10,216,10,61">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView AllowsColumnReorder="False">
                        <GridViewColumn Header="Time left" CellTemplate="{StaticResource timeLeftTemplate}" Width="200"/>
                        <GridViewColumn Header="Name" CellTemplate="{StaticResource nameTemplate}" Width="125"/>
                        <GridViewColumn Header="Date" CellTemplate="{StaticResource dateTemplate}" Width="149"/>

                        <GridView.ColumnHeaderContainerStyle>
                            <Style TargetType="GridViewColumnHeader">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                                <Setter Property="FontFamily" Value="Arial"/>
                                <Setter Property="FontSize" Value="11"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </GridView.ColumnHeaderContainerStyle>
                    </GridView>
                </ListView.View>
            </ListView>
            <StackPanel x:Name="bottom_panel" Orientation="Vertical" Margin="10,644,10,10">
                <StackPanel x:Name="selected_ctrls_panel" Orientation="Horizontal">
                    <Button x:Name="delete_selected_btn" IsEnabled="False" Content="Delete selected" FontFamily="Arial" FontSize="10" FontWeight="Bold" Foreground="White" Background="#052099" BorderThickness="0" Click="delete_selected_btn_Click" Width="140"/>
                    <Button x:Name="unpin_selected_btn" Margin="5, 0, 0, 0" Content="Unpin selected" IsEnabled="False" FontFamily="Arial" FontSize="10" FontWeight="Bold" Foreground="White" Background="#052099" BorderThickness="0" Click="unpin_selected_btn_Click" Width="74"/>
                    <Button x:Name="unfavourite_selected_btn" Margin="5, 0, 0, 0" Content="Unfavourite selected" IsEnabled="False" FontFamily="Arial" FontSize="10" FontWeight="Bold" Foreground="White" Background="#052099" BorderThickness="0" Click="unfavourite_selected_btn_Click" Width="106"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Viewbox>
</Window>
